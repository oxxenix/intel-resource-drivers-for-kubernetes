apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: system:openshift:scc:intel-qat-resource-driver
rules:
- apiGroups:
  - security.openshift.io
  resourceNames:
  - intel-qat-resource-driver
  resources:
  - securitycontextconstraints
  verbs:
  - use
